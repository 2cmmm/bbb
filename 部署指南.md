# 解忧树洞 - 永久部署指南

## 概述

解忧树洞是一个公司员工匿名吐槽心事、分享情绪的 Web 应用。
- 员工匿名发送留言到后台
- 管理员凭密码登录后台，对不正当信息进行删除管理
- 数据存储在云端 PostgreSQL 数据库，永久保存
- 部署到 Vercel，获得永久免费域名

## 技术架构

```
浏览器 → Vercel（前端 + API）→ Neon PostgreSQL（数据库）
```

| 组件 | 服务 | 费用 |
|---|---|---|
| 前端 HTML | Vercel 静态托管 | 免费 |
| 后端 API | Vercel Serverless Functions | 免费 |
| 数据库 | Neon PostgreSQL | 免费（0.5GB） |

## 第一步：认领 Neon 数据库（2分钟）

1. 在浏览器打开认领链接：
   ```
   https://neon.new/claim/019fb0aa-7e05-77fc-a863-c37fede7b0f9
   ```

2. 用 **GitHub** 或 **Google** 账号登录

3. 认领后数据库变成永久的，你会看到项目名为 `green-sunset-22888082`

4. 数据库连接串（请记下来，后续要用）：
   ```
   postgresql://neondb_owner:npg_8jWxGPhOdU0M@ep-damp-hill-aykqd8bu.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require
   ```

> ⚠️ 认领后建议去 Neon 控制台重置密码（Settings → Roles → Reset Password），安全第一。

## 第二步：部署到 Vercel（3分钟）

### 方式 A：用 Vercel CLI 部署

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 进入项目目录
cd shudong-vercel

# 3. 登录 Vercel
vercel login

# 4. 部署（设置环境变量）
vercel --env DATABASE_URL="postgresql://neondb_owner:npg_8jWxGPhOdU0M@ep-damp-hill-aykqd8bu.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require" --env ADMIN_PASSWORD="你的密码"

# 5. 生产部署
vercel --prod
```

部署成功后会给你一个 `https://xxx.vercel.app` 的永久链接。

### 方式 B：通过 GitHub + Vercel 网页部署

1. 把 `shudong-vercel` 目录上传到你的 GitHub 仓库
2. 打开 [vercel.com](https://vercel.com)，用 GitHub 登录
3. 点击 "Import Project" → 选择你的仓库
4. 在 Environment Variables 中添加：

| 变量名 | 值 |
|---|---|
| `DATABASE_URL` | `postgresql://neondb_owner:npg_8jWxGPhOdU0M@ep-damp-hill-aykqd8bu.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require` |
| `ADMIN_PASSWORD` | `你设置的管理员密码` |

5. 点击 Deploy

### 方式 C：部署到 Render.com（备选）

如果你的 Neon 数据库用的是 pooler 连接（带 `-pooler`），Vercel 的 Serverless Functions 可能会有冷启动超时问题。这时可以部署到 Render：

1. 打开 [render.com](https://render.com)，用 GitHub 登录
2. 创建 "Web Service" → 选择仓库
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `python app.py`
5. 环境变量同上

## 项目文件说明

```
shudong-vercel/
├── api/
│   └── index.py          # Vercel Serverless Function（所有API）
├── public/
│   └── index.html         # 前端页面
├── vercel.json            # Vercel 路由配置
└── requirements.txt       # Python 依赖

shudong/                    # 本地 Flask 版本（备份）
├── app.py
├── static/index.html
└── requirements.txt
```

## 管理员使用

1. 打开你的 Vercel 域名
2. 点击右下角 🔐 按钮
3. 输入你设置的管理员密码
4. 进入后台：查看所有留言，点击「删除这条」移除不当内容

## 常见问题

**Q: 免费额度够用吗？**
A: Neon 免费版 0.5GB 存储，Vercel 免费版每月 100GB 流量、1000 次函数调用。小范围使用完全足够。

**Q: 数据库连接失败？**
A: 确认 `DATABASE_URL` 环境变量正确，且 Neon 项目没有被暂停。免费项目 30 天无活动可能暂停，去控制台点一下 Resume 即可。

**Q: 留言会丢失吗？**
A: 不会。数据存在 Neon PostgreSQL 中，除非管理员主动删除。即使 Vercel 部署被删除，数据库数据依然保留。

**Q: 怎么改管理员密码？**
A: 在 Vercel 控制台 → Settings → Environment Variables → 修改 `ADMIN_PASSWORD` → 重新部署。

**Q: 想用本地 Flask 版本？**
A: `shudong/` 目录保留了本地版本，`python app.py` 即可启动（用 SQLite 存数据）。
